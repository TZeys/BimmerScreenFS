Build photos.

| File | What |
|------|------|
| `installed-in-car.jpg` | the prototype fitted in the centre vent panel, engine off |

Originals off the phone are HEIC, which GitHub will not render in a README, so they get converted to
JPEG with the EXIF dropped before going in here. `.heic` is gitignored for that reason.
